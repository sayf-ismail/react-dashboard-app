import React from 'react';
import {Button} from 'react-bootstrap'

function TaskList(props) {
  const myListItems = props.myItems;

  return <div>
      <h3>All Tasks!</h3>
      <section>

      </section>
    </div>
}

export default TaskList;